<Page x:Class="LabProj.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LabProj.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Login">
    <Grid>
        <TextBlock 
            Text="АВТОРИЗАЦИЯ"
            Style="{DynamicResource TitleTextBox}"/>
        <WrapPanel
            Style="{DynamicResource LoginWrapPanel}">
            <TextBox 
                x:Name="LogBox"
                Style="{DynamicResource LoginTextBox}"/>
            <PasswordBox
                x:Name="PassBox"
                Style="{DynamicResource LoginPasswordBox}"/>
            <Button
                Style="{DynamicResource LogInButton}"
                Click="Log"/>
        </WrapPanel>
    </Grid>
</Page>
